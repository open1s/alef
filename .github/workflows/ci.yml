name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Install cargo-machete and cargo-sort
        run: |
          cargo install cargo-machete --locked
          cargo install cargo-sort --locked

      - name: Install cargo-deny and taplo-cli
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-deny,taplo-cli

      - name: Check unused dependencies
        run: cargo machete

      - name: Security and license audit
        run: cargo deny check

      - name: Check Cargo.toml dependency sorting
        run: cargo sort --check --workspace

      - name: Check TOML formatting
        run: taplo check --exclude 'Cargo.toml' --exclude '**/Cargo.toml'

  test:
    name: Test (${{ matrix.os }})
    needs: validate
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Run tests
        run: cargo test --workspace

  build:
    name: Build release
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build release binary
        run: cargo build --release --package alef-cli

      - name: Verify binary
        run: ./target/release/alef --help
